# Data preparation pipeline

TODO for all steps

To generate the rasters per season and cause:

```bash
python data_preparation/generate_season_cause_output.py --root_dir <ROOT_DIR> --hex_id <HEX_ID>
```